// Worker entry point — placeholder for future background job processing
// (e.g., retry failed webhook deliveries, cleanup expired idempotency keys)

export async function startWorker(): Promise<void> {
  console.log("Worker started (no-op placeholder)");
}
